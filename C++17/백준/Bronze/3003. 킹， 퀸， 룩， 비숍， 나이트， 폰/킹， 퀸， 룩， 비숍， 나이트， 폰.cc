#include <iostream>

using namespace std;

int main(void) {
	int k = 0, q = 0, l = 0, v = 0, n = 0, p = 0;
	cin >> k >> q >> l >> v >> n >> p;
	cout << 1 - k << " " << 1 - q << " " << 2 - l << " " << 2 - v << " " << 2 - n << " " << 8 - p << endl;
	return 0;
}